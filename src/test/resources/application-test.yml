spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  redis:
    host: localhost
    port: 6379

flashsale:
  rate-limit:
    enabled: false
  order:
    async-enabled: false

logging:
  level:
    com.flashsale: DEBUG
